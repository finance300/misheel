node_modules/
dist/
.DS_Store
.env
.env.*
*.log
coverage/
.vscode/
.idea/

# Source workbook stays local — JSON is regenerated from it via
# `python3 scripts/extract-fund-data.py`. Extracted JSON in src/data/ IS
# committed so Vercel builds can import it; protect the deployment with
# Vercel's password / SSO protection since the JSON contains investor PII.
*.xlsx
